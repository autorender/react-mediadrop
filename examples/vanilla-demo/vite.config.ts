import { defineConfig } from "vite";

// This demo talks to a real backend (see ../../test-server) instead of a
// dev-server mock — see src/transports.ts's VITE_API_BASE_URL / TRANSPORTS.
export default defineConfig({});
