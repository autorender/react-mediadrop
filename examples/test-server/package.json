{
	"name": "test-server",
	"private": true,
	"version": "0.0.0",
	"description": "Real backend for examples/react-demo — xhr and tus work zero-config; S3 routes activate once you set AWS_* env vars.",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"dev": "node --watch --env-file-if-exists=.env src/index.js",
		"start": "node --env-file-if-exists=.env src/index.js"
	},
	"dependencies": {
		"@aws-sdk/client-s3": "^3.700.0",
		"@aws-sdk/s3-request-presigner": "^3.700.0",
		"@tus/file-store": "^1.5.1",
		"@tus/server": "^1.9.1",
		"cors": "^2.8.5",
		"express": "^5.1.0"
	}
}
