{
	"name": "test-server",
	"private": true,
	"version": "0.0.0",
	"description": "Real backend for examples/react-demo's xhr-upload transport.",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"dev": "node --watch --env-file-if-exists=.env src/index.js",
		"start": "node --env-file-if-exists=.env src/index.js"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^5.1.0"
	}
}
